# connector.py uses only the Python standard library (urllib, json,
# argparse, pathlib, dataclasses) — nothing to install. This file exists
# so CAT/future projects can `pip install -r requirements.txt` as part of
# their normal setup without it being a no-op that surprises anyone.
